export enum RevealStatus {
    Revealed = "REVEALED",
    Unrevealed = "UNREVEALED",
}

export enum GuessStatus {
    Correct = "CORRECT",
    Wrong = "WRONG",
    Pending = "PENDING"
}

export enum Genders {
    Girl = "girl",
    Boy = "boy"
}
